spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payway
    username: postgres
    password: 11111111
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update